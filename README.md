# Experiment‑8 – Flask Student Service

#Learning Outcomes:
1. Understand Flask basics
2. Creating an application factory
3. Build a simple RESTful API
4. Handle HTTP methods (GET, POST, PUT, DELETE)
5. Send and receive JSON payloads
6. Explore in‑memory data management
7. Use Python lists to simulate data storage

## Aim:
Create backend server in Python (Flask).
Create CRUD APIs/Endpoints for students 
Store students data in an array variable in-memory only.
Test using POSTMAN.

---

## 🛠️ Requirements
- Python 3.10+ (the code targets CPython 3.13 in the virtual environments)
- `pip` for installing dependencies

Dependencies are listed in `requirements.txt`:
flask
gunicorn
